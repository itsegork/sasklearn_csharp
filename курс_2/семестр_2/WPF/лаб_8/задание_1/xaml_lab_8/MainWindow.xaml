<Window x:Class="xaml_lab_8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:xaml_lab_8"
        mc:Ignorable="d"
        Title="Задание 1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <StackPanel x:Name="rootElement" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button>Обычная кнопка</Button>
            <TextBox>Текстовое поле</TextBox>
            <StackPanel Orientation="Horizontal">
                <CheckBox>Красный</CheckBox>
                <CheckBox>Зеленый</CheckBox>
                <CheckBox>Синий</CheckBox>
            </StackPanel>
            <Rectangle Width="200" Height="30" Fill="Wheat" Stroke="Brown" StrokeThickness="2" RadiusX="15" RadiusY="15" />
            <Slider x:Name="scaleSlider" Grid.Row="1" Minimum="0.5" Maximum="2.0" Value="1.0" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>